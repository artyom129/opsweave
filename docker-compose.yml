services:
  opsweave:
    build: .
    ports:
      - "8000:8000"
    environment:
      OPSWEAVE_DB_PATH: /app/data/opsweave.db
      OPSWEAVE_SECRET_KEY: ${OPSWEAVE_SECRET_KEY:-replace-me}
    volumes:
      - ./data:/app/data
